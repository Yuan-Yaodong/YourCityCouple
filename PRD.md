# YourCityCouple PRD（当前版本）

> 更新日期：2026-03-07  
> 适用范围：微信小程序 + Web（GitHub Pages）

## 1. 产品定位

`YourCityCouple` 是一个“轻测试 + 情绪价值”工具。用户通过 10 道选择题，在约 1 分钟内获得开年旺城建议，并收到可分享、可执行、可复访的个性化内容。

## 2. 目标与边界

### 2.1 当前目标
- 提供稳定、有趣、可解释的城市推荐结果。
- 增强“被理解感”：结果不只是城市名，还要有人格、运势、行动建议。
- 保持双端主流程一致（小程序为主，Web 为演示与传播补充）。

### 2.2 非目标（当前阶段）
- 不做支付与商业化闭环（打赏/虚拟支付暂不纳入）。
- 不接入需要用户隐私授权的外部行为数据（评论、社交轨迹等）。

## 3. 目标用户
- 18-35 岁，有节日出行/放松诉求的用户。
- 喜欢人格测试、轻互动、社交分享内容的用户。
- 希望获得“轻疗愈 + 具体建议”的用户。

## 4. 当前功能范围（已实现）

### 4.1 题库与结果规模
- 题目：14 题（其中 MBTI 与生肖题为多选项自报题）。
- 城市：23 城（以 `utils/data.js` 为准）。

### 4.2 核心流程
1. 首页进入测试，可续做中断测试。
2. 答题页支持返回上一题修改，过程有即时鼓励反馈。
3. 结果页首屏给出“一句话结论”，详细区可展开。
4. 分享动作分离：微信分享 / 生成海报 / 复制长文案。
5. 回访能力：历史记录、今日好运仪式、连续点亮。

### 4.3 关键体验特性
- 第 7 题“赛博签文”来源稳定（同答案下文案与能量值不漂移）。
- 第 8 题已改为主观“旅途默契题”，避免来源推导与选项冲突。
- 结果解释为证据驱动（展示用户真实命中项）。

## 5. 结果计算逻辑（当前口径）

### 5.1 计算组成
- 基础分：选项对城市的显式权重累加。
- 关联分：
  - MBTI 性格匹配加分（小幅修正）。
  - 生肖/运势匹配加分（小幅修正）。
- 平衡分：城市曝光归一化系数（抑制过热、抬升长尾）。
- 同分机制：细分分 + 命中题数 + 稳定 tie-breaker。

### 5.2 设计原则
- 优先保持题目语义与结果一致性。
- 关联加分不覆盖主问卷意图，只做“纠偏”。
- 保证本地可计算、可复现、无需外部隐私数据。

## 6. 数据、埋点与实验

### 6.1 本地数据
- `answers`：答题进度
- `testResult`：最近结果
- `historyResults`：历史结果
- `dailyRitual`：点亮记录
- `eventLogs`：埋点日志
- `abVariants`：实验分流记录

### 6.2 核心埋点
- `test_start` / `question_answered` / `test_complete` / `result_view`
- `quiz_resume_entry` / `quiz_prev_question` / `quiz_drop_at_question`
- `click_wechat_share_button` / `click_generate_poster` / `click_copy_result`

### 6.3 A/B 实验
- `share_copy_v2`：分享文案（warm/direct/relation）
- `result_button_order_v1`：结果页按钮顺序

## 7. 非功能要求
- 页面流畅，无明显卡顿。
- 异常不阻塞主链路（埋点失败不影响功能）。
- 小程序与 Web 主流程一致。
- 结果逻辑稳定、可解释、可追踪。

## 8. 当前质量状态（结论）
- 核心链路可用，双端功能基本对齐。
- 语义违和问题已显著减少（尤其 Q8/Q10）。
- 城市分布已从“极端头重脚轻”收敛到可接受区间，但仍有优化空间。

质量产物：
- 城市偏置热力图（抽样）：`docs/city-bias-heatmap.md`
- 项目待办与未来优化展望：`docs/project-backlog.md`

## 9. 验收标准（本阶段）
- [ ] 用户可完成 14 题并稳定出结果。
- [ ] 用户可续做中断测试并可回退改题。
- [ ] 结果页首屏 3 秒可读，详细区可展开。
- [ ] 分享/海报/复制文案可用且语义清晰。
- [ ] 小程序与 Web 主链路体验一致。
- [ ] 结果逻辑不存在明显“语义违和”高频案例。
